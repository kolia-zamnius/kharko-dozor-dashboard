# next-auth@5-beta peer-pins nodemailer@^7 and @simplewebauthn@^9, but we run
# nodemailer@^8 (identical SMTP behaviour) and @simplewebauthn@^13 (required
# for the current passkey flow). Revisit when next-auth@5 ships stable.
legacy-peer-deps=true
